# Aprofundando Git Parte I

Nessa aula vimos alguns outros comandos. 

- `git diff`: realiza comparações
- `git show`: mostra objetos

Também vimos o conceito de Branch, com comandos:

- `git branch nome-da-branch`: cria branch
- `git branch -a`: lista branch
- `git checkout nome-da-branch`: troca de branch